@model Metadados.Models.Mapeamento

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h4>Mapameamento</h4>

<h3>Tem certeza que deseja deletar este objeto?</h3>
<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.sky_pipeline)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.sky_objeto_destino)
        </dd>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.sky_objeto_origem)
        </dd>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.dsc_mapeamento_colunas)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="sky_mapeamento" />
        <input type="submit" value="Delete" class="btn btn-danger" /> 
        <a asp-action="Index">Back to List</a>
    </form>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}